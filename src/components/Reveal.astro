---
interface Props {
  as?: string;
  delay?: number;
  class?: string;
}
const { as: Tag = "div", delay = 0, class: className = "" } = Astro.props;
---

<Tag class={className || undefined} data-reveal style={delay ? `--reveal-delay:${delay}ms` : undefined}>
  <slot />
</Tag>
